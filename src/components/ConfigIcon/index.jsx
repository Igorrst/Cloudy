import { ConfigIconContainer } from './styles';
import configIcon from '../../assets/config.png';

const ConfigIcon = () => {
  return <ConfigIconContainer src={configIcon} alt="Configuração" />;
};

export default ConfigIcon;